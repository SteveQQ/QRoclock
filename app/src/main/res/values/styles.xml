<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/material_deep_purple_600</item>
        <item name="colorPrimaryDark">@color/material_deep_purple_800</item>
        <item name="colorAccent">@color/material_orange_A400</item>
        <item name="android:textColorPrimaryInverse">@color/material_light_blue_300</item>
        <item name="android:colorBackground">@color/material_deep_purple_200</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
